import 'package:flutter/material.dart';

class HalamanMap extends StatelessWidget {
  const HalamanMap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Halaman Maps'),
        ),
      ),
    );
  }
}
